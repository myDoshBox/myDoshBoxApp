export interface CoreValueCardProps {
  title: string;
  subtitle: string;
  description: string;
}
export interface TeamsCardProps {
  name: string;
  job_description: string;
  description: string;
  images?: string;
}
